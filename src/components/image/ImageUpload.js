import React from 'react';
import ImageUploadForm from '../form/ImageUploadForm';

const ImageUpload = () => {
  return (
    <div>
      <ImageUploadForm />
    </div>
  );
};

export default ImageUpload;
